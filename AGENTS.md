# Codex Info 開発ルール

## 最重要：完了前の停止・終了を禁止する固定規約

この規約は本ファイル内の通常ガイダンスより優先する。要求台帳、実装、検証、独立評価、監視のいずれかが未完了・未確認・`HOLD`・`INCONCLUSIVE`・FAILである間は、作業を停止して終了扱いにしてはならない。処理を継続し、必要なら新しい担当へ再配置する。

### スレッド・セッションの終了禁止

要求抽出、要件検証、実装、テスト、独立評価、監視のいずれかが未完了・未確認・
`HOLD`・`INCONCLUSIVE`・FAILである間は、主スレッド、監視スレッド、サブエージェントを
自主的に終了してはならない。終了、切断、タイムアウト、応答欠落、割り込みは完了を意味せず、
直ちに `HOLD/INCONCLUSIVE` と記録して同じ要求を新しい担当へ再配置し、処理を継続する。
ユーザーが明示的に停止を求めた場合でも、停止は完了報告ではなく中断記録として扱い、再開時に
未完了の台帳から継続する。固定完了文言を使用できる状態になるまで、スレッドを完了終了してはならない。

未完了を理由にユーザーへ入力・確認・判断を求め、その返答待ちを終了条件にしてはならない。
判断が必要な事項は `open`/`INCONCLUSIVE` として台帳へ記録し、既存資料から確定できる範囲の
抽出、矛盾整理、証拠要件化、独立突合を継続する。ユーザー入力が必要な状態でも、スレッドを
完了扱いにせず、処理継続対象として保持する。

**「100%要求を満たしました。完了しました。」以外の状態で、ユーザーへ入力・確認・判断を
求める行動は禁止する。** 未完了状態で質問を返して処理を終えること、入力待ちを理由にスレッドを
終了すること、入力がないことを完了根拠にすることを認めない。

未完了状態での進捗・状態報告は、完了報告でも処理終了でもない。状態を返した後も、同一の
要求追跡、文書化、独立突合、再配置を続行し、状態報告だけを返してスレッドを区切ってはならない。

**最終応答を `100%要求を満たしました。完了しました。` 以外の文言で終えてはならない。**
この固定文言を使用できない間は、ターンを完了扱いで終えず、要求抽出・実装・検証・独立評価・
監視を継続する。チャット基盤のターン終了、接続断、タイムアウトなど実行環境が強制した停止は、
完了・PASSと偽らず `HOLD/INCONCLUSIVE` として台帳へ記録し、次に実行権が与えられた時点で
質問を返さず、台帳の未完了行から直ちに再開する。

この境界の原子契約は`docs/REQUIREMENTS_LEDGER.md`の`GOV-THREAD-END`、
`GOV-NO-INPUT-END`、`GOV-ESCALATION-100X`とする。`no_api_turn_liveness_claim=0`であり、
turn終了後もagent/processが生存すると偽らず、実際のgoal/agent/reassignment/continuation eventだけを
追跡証拠にする。実eventなしの「継続済み」「再配置済み」、入力待ちからterminalへの遷移を禁止する。

要求抽出を承認した後に、台帳へ明示されていない要求が1件でも発見された場合は、抽出承認を失効させる。現在の抽出候補226件に対する再抽出目標はユーザー契約により226,000件（226件×1,000）とし、全件を原子要求へ分解し、根拠・対象・境界・失敗時保持・UX理由・証拠・独立評価・合否式を埋めるまで実装・テスト・評価・ビルド・インストール・成果物差し替えを再開してはならない。誤記か漏れか不明な場合も漏れとして扱う。件数を減らす判断にはユーザー明示承認と独立監査記録が必要である。

抽出途中で基準件数が増えた場合は、その時点の承認件数を `N` とする。以後の承認後に要求漏れが発見された場合は、再抽出目標を `max(226,000, N × 100)` 件へ拡張し、さらに再発するたびにその時点の `N` へ同じ100倍規則を適用する。件数を増やした後に確認を簡略化してはならない。

### 複数スレッドによる加速と統合ゲート

複数の独立した要求領域は、所有ファイル・入力・受入ゲートを重複させない bounded なサブスレッドへ並列分担してよい。
各スレッドは `docs/AGENT_REQUIREMENTS_TRACKER.md` に作業ID、対象ID、所有範囲、成果物、独立判定を登録し、別領域の変更を戻してはならない。
主担当は同じ作業ツリーの差分を統合し、全スレッドのraw結果・監査結果・artifact SHAを突合する。並列実行したこと自体は完了根拠にならず、1スレッドでも未報告、タイムアウト、`HOLD`、`INCONCLUSIVE`、FAILなら全体を停止せず再配置して継続する。
コード実装へ進む前は要求抽出スレッドの独立PASS、実装後は各製品領域スレッドの同一SHA独立PASSを必須とし、並列の部分PASSを全体PASSへ合成してはならない。

完了として最終報告できるのは、全要求・全ゲート・独立評価・実環境証拠がPASSした場合だけであり、固定文言は **「100%要求を満たしました。完了しました。」** とする。それ以外の状態で「完了しました」「対応しました」「納品できます」などの終了表現を使用することを禁止する。未達のままセッションやサブエージェントが終了した場合は、完了ではなく `INCONCLUSIVE/HOLD` として記録し、継続処理の対象とする。

## UI変更の完了条件

このプロジェクトのUI変更は、コンパイルが通っただけでは完了としない。実際にX Windowへ表示し、最新の画面キャプチャを目視レビューしてから完了判定する。

実装担当者が少しでも「まだ不完全かもしれない」「別状態を確認できていない」「見た目の判断に迷いがある」と感じている間は、ユーザーへ完了として返してはならない。未確認事項をユーザーに評価させることも禁止する。まず実装側で確認経路を作り、問題を潰してから報告する。

### 必須手順

1. `cargo fmt --check`、`cargo check`、`cargo test`、`cargo build --release`を実行する。
2. `run.sh`で実際のX Windowを起動する。
3. 起動したプロセスだけを特定してキャプチャする。過去のウィンドウを再利用した画像は検証に使わない。
4. 画像を画面全体として目視し、以下のチェックリストを一つずつ確認する。
5. 修正を行った場合は、修正前の画像を再利用せず、必ず新しい画像を撮り直す。

状態別の目視確認には、通信状態に依存しないプレビュー起動を使う。`CODEX_INFO_PREVIEW=auth|normal|warning|error ./run.sh`で各状態を表示し、通常のCodex認証状態を変更せずに画像を撮る。
最小幅の確認は、`CODEX_INFO_PREVIEW_SIZE=700x480 CODEX_INFO_PREVIEW=normal ./run.sh`のようにサイズ指定を加えて行う。残量境界値は`CODEX_INFO_PREVIEW=zero|full`で確認する。

キャプチャできない、画像を処理できない、または目視確認できない場合は、UIを合格・完了として報告しない。その場合は未検証として明示する。

## 目視レビュー必須チェックリスト

### 幾何・余白

- 外枠からの左・右・上・下の余白を実測し、意図した値と一致している。
- Header、主サマリー、ゲージ、補助情報、Statusの左右端が意図したグリッドに揃っている。
- 画面幅を変えたとき、右端のはみ出し、段差、下端クリップがない。
- 余白が過剰または不足していない。単に「収まっている」だけでは合格にしない。

### データ表現

- プログレスバーとゲージの塗りつぶしは、必ず左端から右端へ進む。
- 0%、中間値、100%の3状態を確認する。中間値が中央寄せになっていないことを画像で確認する。
- 7日ゲージは、端数セルの明るい部分と暗い部分の向きが正しい。
- 表示文言とゲージの意味が一致している。
- モデル別履歴は週間リセット開始から次回リセットまでの利用期間をアプリ内タイトルに含め、その期間内のローカルログ由来の入力・キャッシュ入力・出力トークン数を表示する。各カテゴリをトークン列と予想ドル列に分離し、数値列はそれぞれ独立して右揃え・3桁区切りにする。クレジット換算は表示しない。履歴がない分類は行ごと表示しない。
- 0日・0時間のような不自然な単位を表示せず、日数が0になった状態も自然な日本語になっている。
- 同じ情報やパーセンテージを画面内で重複表示していない。
- リセットまでの具体的な日・時・分は7日ゲージの下段だけに置く。主サマリーは残り利用枠を主役にし、同じカウントダウン文言を再掲しない。
- 画面内の全テキストを意味単位で棚卸しし、各事実に表示所有者が1つだけある。言い換えによる重複（例: 更新周期をヘッダーと状態帯の両方に表示）もFAILとする。
- 新しい表示を追加する前に、`DESIGN.md`の「情報の所有権と重複防止」表へ所有者・表示場所・表示文言を登録する。表に登録できない補助文言は追加しない。
- 明示的に要求されていない派生値・説明文は追加しない。既存表示と同じ意味の値は、別のラベルや言い換えで重複表示しない。

### 視覚階層・タイポグラフィ

- 最重要情報が最初に目に入り、補助情報が主役と競合していない。
- 日本語が欠字・文字化け・過度な細字・過度な小字になっていない。
- ラベル、主数値、単位、状態文のサイズとコントラストに役割の差がある。
- 同じサマリーの要素が不自然な大きな空白で分断されていない。
- OSのタイトルバー、アプリ内タイトル、ロゴが重複していない。

### 状態別確認

- 未認証、認証済み通常、残量警告、リセット前後警告、取得エラーをそれぞれ確認する。
- 状態変化で背景、Status、ボタン、既存値の保持が設計どおり変わる。
- エラー文が長くなっても、主要情報の押し下げ・クリップ・レイアウト崩壊がない。

## 独立評価ゲート

UI実装担当者の自己確認だけでは完了にしない。別の評価担当に、実装者の結論を渡さず、次を独立確認させる。

- 最新画像を目視したか
- 上記チェックリストの各項目に証拠があるか
- 静的な幅・高さ計算だけでなく、塗り方向・整列・視覚階層を確認したか
- 問題が一つでも残っていればFAILとしたか
- 画像内の文言を読み上げ、同じ事実の重複を意味レベルで検出したか（文字列一致だけに頼らない）

実装者自身が設計上の課題を一つでも認識している場合、独立評価がPASSでも完了にはしない。課題を修正し、修正後の新しい画像で再評価する。

静的評価がPASSでも、実画像に問題があれば画像評価を優先する。特にSlintでは、子要素に`x`を指定しないと意図せず中央配置されることがあるため、バー内部の塗りつぶしRectangle、セル内の進捗Rectangle、ラベルの開始位置を必ず画像で確認する。

## 完了報告の形式

完了報告には、少なくとも次を含める。

- 実画像を確認したサイズと状態
- 左右上下余白、主要部品の整列、ゲージ塗り方向の確認結果
- 実行した検証コマンドと結果
- 未確認の状態や残課題（あれば完了扱いにしない）

「ビルド成功」「枠内に収まる」「静的評価PASS」だけを理由に、UIを設計完了と報告してはならない。

## 100%完了の強制制約

「完了」「納品可能」「100%要求達成」と報告できるのは、次の条件をすべて満たした場合だけとする。

**最終応答を完了扱いで終了できる固定文言は「100%要求を満たしました。完了しました。」のみとする。** この固定文言を使用できない状態では、最終応答で作業を終了してはならず、未達項目の実装・検証・監視を継続する。`HOLD`、`INCONCLUSIVE`、FAIL、未確認、環境制約が一つでも残る場合に「完了しました」「対応しました」「納品できます」などの終了表現へ置き換えることを禁止する。

**100%要求達成以外の状態で、実装・検証・監視の処理を停止して終了することは、本プロジェクトにおける最大レベルの規約違反とする。** 未完了のまま担当者・サブエージェント・監視スレッドが終了した場合は、完了ではなく `INCONCLUSIVE/HOLD` として直ちに記録し、新しい担当へ再配置して処理を継続する。タイムアウト、応答欠落、環境制約、セッション終了を完了の根拠にしてはならない。主担当は、100%達成の独立証拠と全ゲートPASSを取得するまで、停止・完了報告・納品報告を行ってはならない。

- 要求台帳の全行が同一source freeze・同一release artifact lineage・fresh独立証拠に基づく
  `current release PASS`である。旧世代または作成者自己判定の`verified`、`implemented`、
  `historical evidence only`、`contract authored`、`product pending`は現行PASSへ読み替えない。
- 未確認、未実装、`HOLD`、`INCONCLUSIVE`、FAIL、環境制約による代替証拠が一つも残っていない。
- `completion_guard.sh`、要求抽出ゲート、回帰ゲート、データ保護ゲートが同一成果物に対してPASSである。
- 実装担当者とは別の独立サブエージェントが、全要求行と全受入証拠をPASSと判定している。
- UI・Windows導入・実プロセス・DB・daemon・異常系について、要求された実環境証拠が最新成果物SHAに結び付いている。
- 完了直前に監視スレッドを再実行し、PASSログを保存している。

上記の一つでも満たさない場合は作業を継続し、完了報告をしてはならない。「私のミスです」などの説明で未達を置き換えてはならない。全条件を満たした場合のみ、最終報告に「100%要求を満たしました」と明記し、証拠ファイル・コマンド・SHAを併記する。条件を満たしていない状態でこの文言を使用することは禁止する。
