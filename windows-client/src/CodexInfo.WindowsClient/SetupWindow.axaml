<!-- Copyright (C) 2026 salty919 -->
<!-- SPDX-License-Identifier: GPL-3.0-only -->
<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:CodexInfo.WindowsClient.ViewModels"
        x:Class="CodexInfo.WindowsClient.SetupWindow"
        x:DataType="vm:SetupViewModel"
        Width="760" Height="680" MinWidth="680" MinHeight="600"
        WindowStartupLocation="CenterOwner" CanResize="False"
        WindowDecorations="None" ExtendClientAreaToDecorationsHint="True" ExtendClientAreaTitleBarHeightHint="0"
        Title="Codex Info Setup" FontFamily="{StaticResource JapaneseFont}"
        Background="#0E141E" Foreground="#E9EFF8">
  <Window.Styles>
    <Style Selector="TextBlock.label"><Setter Property="FontSize" Value="13" /><Setter Property="Foreground" Value="#A8B7CA" /></Style>
    <Style Selector="Border.surface"><Setter Property="Background" Value="#151F2D" /><Setter Property="BorderBrush" Value="#2C4258" /><Setter Property="BorderThickness" Value="1" /><Setter Property="CornerRadius" Value="14" /></Style>
    <Style Selector="Button.primary"><Setter Property="Background" Value="#236B9E" /><Setter Property="BorderBrush" Value="#61B4EB" /><Setter Property="Foreground" Value="#F5FAFF" /><Setter Property="Padding" Value="16,8" /></Style>
    <Style Selector="Button.primary:disabled"><Setter Property="Background" Value="#1B2735" /><Setter Property="BorderBrush" Value="#304052" /><Setter Property="Foreground" Value="#8EA1B5" /></Style>
    <Style Selector="Button.secondary"><Setter Property="Background" Value="#1B2A3A" /><Setter Property="BorderBrush" Value="#36516B" /><Setter Property="Foreground" Value="#E9EFF8" /><Setter Property="Padding" Value="16,8" /></Style>
  </Window.Styles>
  <Grid Margin="28" RowDefinitions="Auto,Auto,*,Auto" RowSpacing="18">
    <Grid ColumnDefinitions="*,Auto" Background="Transparent" PointerPressed="OnTitlePointerPressed">
      <StackPanel Orientation="Horizontal" Spacing="12"><Border Width="42" Height="42" CornerRadius="12" Background="#1C5D91" BorderBrush="#5EA7E5" BorderThickness="1"><TextBlock FontSize="22" Text="◈" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border><StackPanel VerticalAlignment="Center"><TextBlock FontSize="25" FontWeight="SemiBold" Text="{Binding Texts.SetupTitle}" /><TextBlock Classes="label" Text="{Binding Texts.Setup}" /></StackPanel></StackPanel>
      <Button Grid.Column="1" Background="Transparent" BorderBrush="Transparent" Foreground="#A8B7CA" Click="OnClose" AutomationProperties.Name="{Binding Texts.Close}" Content="×" FontSize="22" ToolTip.Tip="{Binding Texts.Close}" />
    </Grid>
    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
      <Border Width="10" Height="10" CornerRadius="5" Background="#5EA7E5" /><TextBlock Classes="label" Text="{Binding Texts.StepConnection}" /><Border Width="10" Height="10" CornerRadius="5" Background="#36516B" Margin="14,0,0,0" /><TextBlock Classes="label" Text="{Binding Texts.StepAuth}" /><Border Width="10" Height="10" CornerRadius="5" Background="#36516B" Margin="14,0,0,0" /><TextBlock Classes="label" Text="{Binding Texts.StepDone}" />
    </StackPanel>
    <Border Grid.Row="2" Classes="surface" Padding="22">
      <!-- Keep a real content gutter for the vertical scrollbar.  Avalonia's
           overlay scrollbar can otherwise paint over the last glyph of a
           wrapped Japanese/Arabic line at the minimum width. -->
      <ScrollViewer Padding="0,0,18,0" HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" Margin="0,0,2,0">
          <TextBlock Classes="label" Text="{Binding Texts.SetupIntro}" TextWrapping="Wrap" />
          <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Texts.ConnectionGuide}" />
          <TextBlock Classes="label" Text="{Binding Texts.ConnectionGuideBody}" TextWrapping="Wrap" />
          <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
            <StackPanel Spacing="5">
              <TextBlock Classes="label" Text="{Binding Texts.ConnectionProfileLabel}" />
              <ComboBox ItemsSource="{Binding ConnectionProfileOptions}"
                        SelectedValue="{Binding SelectedConnectionProfile, Mode=TwoWay}"
                        SelectedValueBinding="{Binding Id}"
                        AutomationProperties.Name="{Binding Texts.ConnectionProfileLabel}"
                        ToolTip.Tip="{Binding Texts.ConnectionProfileLabel}">
                <ComboBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding Label}" /></DataTemplate></ComboBox.ItemTemplate>
              </ComboBox>
            </StackPanel>
            <StackPanel Grid.Column="1" Spacing="5">
              <TextBlock Classes="label" Text="{Binding Texts.ConnectionSelectorLabel}" />
              <ComboBox ItemsSource="{Binding ConnectionSelectorOptions}"
                        SelectedItem="{Binding SelectedConnectionSelector, Mode=TwoWay}"
                        PlaceholderText="{Binding Texts.ConnectionSelectorPlaceholder}"
                        AutomationProperties.Name="{Binding Texts.ConnectionSelectorLabel}"
                        ToolTip.Tip="{Binding Texts.ConnectionSelectorLabel}" />
            </StackPanel>
          </Grid>
          <TextBlock Classes="label" Text="SSH" />
          <StackPanel Spacing="5">
            <TextBlock Classes="label" Text="{Binding Texts.SshConfigAliasLabel}" />
            <ComboBox ItemsSource="{Binding SshConfigAliases}" SelectedItem="{Binding SelectedSshConfigAlias, Mode=TwoWay}" PlaceholderText="{Binding Texts.SshConfigAliasPlaceholder}" AutomationProperties.Name="{Binding Texts.SshConfigAliasLabel}" />
          </StackPanel>
          <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
            <StackPanel Spacing="5">
              <TextBlock Classes="label" Text="{Binding Texts.SshUserLabel}" />
              <TextBox Text="{Binding SshUser, Mode=TwoWay}" PlaceholderText="{Binding Texts.SshUserPlaceholder}" AutomationProperties.Name="{Binding Texts.SshUserLabel}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Spacing="5">
              <TextBlock Classes="label" Text="{Binding Texts.SshHostLabel}" />
              <TextBox Text="{Binding SshHost, Mode=TwoWay}" PlaceholderText="{Binding Texts.SshHostPlaceholder}" AutomationProperties.Name="{Binding Texts.SshHostLabel}" />
            </StackPanel>
          </Grid>
          <Border Background="#0A111A" CornerRadius="8" Padding="12">
            <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="8">
              <TextBlock FontFamily="Consolas" MinWidth="0" Text="{Binding SshCommand}" TextWrapping="Wrap" />
              <TextBlock Grid.Row="1" Classes="label" Foreground="#E7C26A" Text="{Binding SshStatusText}" TextWrapping="Wrap" />
              <Button Grid.Row="2" HorizontalAlignment="Right" Classes="secondary" Click="OnCopySsh" AutomationProperties.Name="{Binding Texts.Copy}" ToolTip.Tip="{Binding Texts.Copy}" Content="{Binding Texts.Copy}" />
            </Grid>
          </Border>
          <TextBlock Classes="label" Text="Linux / WSL" />
          <Border Background="#0A111A" CornerRadius="8" Padding="12">
            <Grid RowDefinitions="Auto,Auto" RowSpacing="8">
              <TextBlock FontFamily="Consolas" MinWidth="0" Text="{Binding ApiCommand}" TextWrapping="Wrap" />
              <Button Grid.Row="1" HorizontalAlignment="Right" Classes="secondary" Click="OnCopyApi" AutomationProperties.Name="{Binding Texts.Copy}" ToolTip.Tip="{Binding Texts.Copy}" Content="{Binding Texts.Copy}" />
            </Grid>
          </Border>
          <Border Background="#101F2D" CornerRadius="8" Padding="14"><Grid ColumnDefinitions="Auto,*" ColumnSpacing="10"><TextBlock Text="●" Foreground="#71C7FF" VerticalAlignment="Top" Margin="0,3,0,0" /><StackPanel Grid.Column="1"><TextBlock FontWeight="SemiBold" Text="{Binding StatusTitle}" /><TextBlock Classes="label" Text="{Binding StatusDetail}" TextWrapping="Wrap" /></StackPanel></Grid></Border>
        </StackPanel>
      </ScrollViewer>
    </Border>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10">
      <Button Classes="primary" IsEnabled="{Binding CanStartSsh}" Click="OnStartOrStopSsh" AutomationProperties.Name="{Binding SshActionText}" ToolTip.Tip="{Binding SshActionText}" Content="{Binding SshActionText}" />
      <Button Classes="secondary" Click="OnRefresh" AutomationProperties.Name="{Binding Texts.AuthCheck}" ToolTip.Tip="{Binding Texts.AuthCheck}" Content="{Binding Texts.AuthCheck}" />
      <Button Classes="secondary" Click="OnClose" AutomationProperties.Name="{Binding Texts.Close}" ToolTip.Tip="{Binding Texts.Close}" Content="{Binding Texts.Close}" />
      <Button Classes="primary" IsEnabled="{Binding CanContinue}" Click="OnContinue" AutomationProperties.Name="{Binding ContinueText}" ToolTip.Tip="{Binding ContinueText}" Content="{Binding ContinueText}" />
    </StackPanel>
  </Grid>
</Window>
